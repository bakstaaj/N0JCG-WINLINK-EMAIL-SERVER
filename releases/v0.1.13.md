# N0JCG Winlink Email Server v0.1.13

## Highlights

- Fixed first-install operator authentication setup when deployment runs over
  non-interactive SSH.
- The deployment script now detects missing operator authentication and collects
  the username and password locally before securely transferring them to the Pi.
- Existing operator authentication remains preserved unless explicitly reset.
- Temporary connectivity and operator-authentication files are removed after
  deployment.

## Validation

- Bash syntax validation passed.
- 44 unit tests passed.
- Release contents exclude generated runtime data and historical release
  archives.
